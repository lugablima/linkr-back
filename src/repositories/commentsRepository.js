// import db from "../db/postgres.js";

function getCommentsByPostId(userId, postId) {
  //
}

function insertComment(userId, postId) {
  //
}

export default {
  getCommentsByPostId,
  insertComment,
};
